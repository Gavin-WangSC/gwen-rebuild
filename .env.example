# Drizzle
DATABASE_URL=file:local.db

# Inherited DashScope development baseline. The deployment provider and final
# environment contract are open; do not build new code around these names yet.
DASHSCOPE_API_KEY=

# Current port default, which is what GWen v1 used:
#   https://batch.dashscope.aliyuncs.com/compatible-mode/v1
# This does not settle whether the runnable slice will use DashScope, ordinary
# concurrent calls, or a provider Batch API.
DASHSCOPE_BASE_URL=
